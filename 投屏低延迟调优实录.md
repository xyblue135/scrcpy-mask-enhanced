# 手游投屏低延迟调优实录：从"画面糊到 480P"到 25ms 跟手

> 记录一次基于 **scrcpy-mask-enhanced**（Qualcomm/Windows 低延迟分支）的手游投屏调优全过程。
> 核心结论：**H265 + 标准编码器 `c2.qti.hevc.encoder` + 720p 虚拟屏**，是"低延迟 + 可用画质"的平衡点。

---

## 一、背景与目标

- 手机：小米（骁龙 8 系），物理屏 **1200×2608**（竖屏 2K，宽仅 1200px）
- 电脑：Windows，显示器 **2560×1440**（横屏 2K）
- 游戏：王者荣耀（Unreal Engine 4 / `com.epicgames.ue4.GameActivity`）
- 目标：**低延迟（跟手）+ 尽量清晰 + 流畅**
- 投屏软件：`scrcpy-mask-enhanced`（自定义 server v4.0）

---

## 二、踩坑全记录（为什么一开始那么糊）

### 1. 主屏投屏"必然糊"——物理分辨率不够

手机物理屏宽只有 **1200px**。投到 2560 宽的显示器：

```
手机画面 1200px  →  显示器 2560px  →  放大 2.13 倍
```

放大就是糊，**任何编码器都救不了**。这是"投主屏（不建虚拟屏）"的根本局限。

### 2. 虚拟屏是"绕开物理屏限制"的唯一办法

通过 `--new-display` 创建**更高分辨率的虚拟显示器**，游戏渲染到虚拟屏，推流分辨率不再受手机物理屏限制。

### 3. 换编码器能救画质，但代价是延迟

| 编码器 | 画质 | 延迟 | 实测 |
|---|---|---|---|
| `c2.qti.hevc.encoder.cq` | ❌ 差（糊） | ✅ 25ms | 低延迟恒定质量，牺牲画质 |
| `c2.qti.hevc.encoder`（标准）| ✅ 好 | 🔶 30ms+ | 完整编码，画质好 |
| `c2.mi.mihc.encoder`（小米）| 🔶 中 | — | **闪退，不稳定，排除** |
| `c2.qti.avc.encoder`（H264）| ✅ 好 | 🔶 略高 | 可用，但编码效率低于 H265 |

> `.cq` 是 Qualcomm 的"低延迟恒定质量"模式，编码时减少参考帧、简化模式决策，**为速度牺牲画质**。

---

## 三、最终选定方案（方案2）

```bash
scrcpy --video-codec=h265 \
       --video-encoder=c2.qti.hevc.encoder \
       --video-bit-rate=20000000 \
       --video-codec-options=vendor.qti-ext-enc-low-latency.enable=1 \
       --max-fps=60 \
       --video-buffer=0 \
       --no-audio \
       --new-display=1280x720/320 \
       --keep-active=false \
       --vd-destroy-content=false \
       --vd-system-decorations=true \
       --start-app=com.android.chrome
```

### 关键参数解析

| 参数 | 值 | 作用 |
|---|---|---|
| `video-codec` | `h265` | HEVC 压缩率高，编码延迟低于 H264 |
| `video-encoder` | `c2.qti.hevc.encoder` | **标准编码器**，画质好（不是 `.cq`）|
| `video-bit-rate` | `20M` | 720p 足够，带宽小 |
| `video-codec-options` | `vendor.qti-ext-enc-low-latency.enable=1` | 高通低延迟开关 |
| `max-fps` | `60` | 帧率非越高越好，60fps 跟手更好 |
| `video-buffer` | `0` | **零缓冲**，低延迟关键 |
| `new-display` | `1280x720/320` | **720p 虚拟屏**，编码压力小 |
| `start-app` | `com.android.chrome` | 测试时用 Chrome，实际可换游戏包名 |

---

## 四、选型过程中学到的核心结论

### 1. 分辨率是画质的"天花板"，编码器是"地板"

```
最终画质 = min(采集/虚拟屏分辨率, 编码器质量, 码率)
```

- 分辨率不够 → 必然糊（放大）
- 编码器差 → 同分辨率下也糊（`.cq` 的教训）
- 码率不够 → 压缩痕迹（块状、涂抹）

### 2. 帧率不是越高延迟越低

`max-fps=120` 反而增加编码排队压力。**低延迟最优通常是 60fps**，不是更高。

### 3. USB 2.0 对 scrcpy 完全够用

- 方案2 码率仅 20Mbps
- USB 2.0 带宽 480Mbps，**只占 4%**
- USB 2.0 vs 3.0 对视频传输**几乎没有区别**

### 4. 低延迟的真正来源

按贡献排序：
1. **USB 有线连接**（vs Wi-Fi，延迟降 50%+）
2. **零缓冲 `--video-buffer=0`**
3. **编码器**（标准 H265 + 低延迟选项）
4. **虚拟屏分辨率**（720p 编码轻）

---

## 五、给你的调优建议

### 找平衡点的顺序

1. 先确认**物理屏分辨率**（`adb shell wm size`）
2. 分辨率不够 → **用虚拟屏**绕开限制
3. 从高分辨率往下调，找到"流畅 + 清晰"平衡点（720p → 900p → 1080p）
4. 画质差 → 换**标准编码器**（去掉 `.cq`）
5. 延迟高 → 确认 **USB 有线 + 零缓冲 + 60fps**

### 常见问题速查

| 现象 | 原因 | 解法 |
|---|---|---|
| 主屏投屏糊 | 物理屏分辨率低，放大 | 用虚拟屏 |
| 开了虚拟屏还糊 | `.cq` 低延迟编码器画质差 | 换标准 `c2.qti.hevc.encoder` |
| 1080p 卡 | 编码/渲染压力大 | 降到 900p / 720p |
| 延迟高 | 可能是 Wi-Fi / 缓冲 | USB 有线 + `--video-buffer=0` |
| 小米 HEVC 闪退 | 编码器不稳定 | 用高通 `c2.qti.*` 系 |

---

## 六、实测数据汇总

| 方案 | 编码器 | 分辨率 | 延迟 | 画质 | 结论 |
|---|---|---|---|---|---|
| 基准 | `hevc.encoder` | 主屏 1200 | — | 糊 | 物理屏分辨率不足 |
| `.cq` | `hevc.encoder.cq` | 900p | 25ms | 糊 | 低延迟但画质差 |
| 方案1 | `hevc.encoder` | 900p | 30ms+ | 好 | 画质好延迟略高 |
| 方案3 | `mi.mihc.encoder` | 900p | — | — | **闪退** |
| **方案2** | `hevc.encoder` | **720p** | 25ms | 可用 | **最佳平衡** ✅ |

---

## 结语

调优投屏，核心是在"分辨率、编码器、码率、帧率、缓冲"五者之间找平衡。**物理屏分辨率不够就用虚拟屏，画质差就换标准编码器，延迟高就保零缓冲 + USB 有线**。

最终方案2（H265 + 标准编码器 + 720p）在 25ms 延迟下提供了可接受的画质，是这套设备配置下的**最佳平衡点**。

---

*本文基于 scrcpy-mask-enhanced 项目 + 小米骁龙 8 系手机实测。不同机型/游戏需结合实际微调。*
